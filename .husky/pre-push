set -e

echo "🔍 Pre-push runs checks only for changed application areas."
echo "  Use HUSKY_FULL_CHECKS=1 for the existing full local checks."

bash .husky/run-checks.sh pre-push

echo "🚀 Push listo."
